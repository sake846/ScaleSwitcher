<Window x:Class="ScaleSwitcher.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:ScaleSwitcher.ViewModels"
        Title="{Binding Title}" Height="420" Width="300"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:SettingsViewModel}">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding TargetDisplayHeader}" FontWeight="Bold" Margin="0,0,0,5" Grid.Row="0"/>
        <ComboBox ItemsSource="{Binding Displays}"
                  SelectedItem="{Binding SelectedDisplay, Mode=TwoWay}"
                  DisplayMemberPath="DisplayName"
                  Margin="0,0,0,10" Grid.Row="1"/>

        <CheckBox Content="{Binding UseCustomDisplayNameText}"
                  IsChecked="{Binding UseCustomDisplayName, Mode=TwoWay}"
                  Margin="0,0,0,8"
                  Grid.Row="2"/>

        <TextBlock Text="{Binding CustomDisplayNameHeader}" Margin="0,0,0,5" Grid.Row="3"/>
        <TextBox Text="{Binding DisplayNameText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 IsEnabled="{Binding UseCustomDisplayName}"
                 Margin="0,0,0,15"
                 Grid.Row="4"/>

        <TextBlock Text="{Binding ScalesHeader}" FontWeight="Bold" Margin="0,0,0,5" Grid.Row="5"/>
        <ScrollViewer Grid.Row="6" VerticalScrollBarVisibility="Auto" Margin="0,0,0,15">
            <ItemsControl ItemsSource="{Binding ScaleOptions}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding DisplayText}" 
                                  IsChecked="{Binding IsSelected, Mode=TwoWay}" 
                                  Margin="0,2"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Button Command="{Binding SaveCommand}" Content="{Binding SaveButtonText}" Width="80" HorizontalAlignment="Right" Grid.Row="7" Padding="5"/>
    </Grid>
</Window>
